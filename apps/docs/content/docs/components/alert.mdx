---
title: Alert
description: A component for displaying critical information or notifications.
styleSource: https://github.com/mijn-ui/mijn-ui-react/blob/main/packages/theme/src/components/alert.ts
---

<ComponentPreview name="alert-example" />

## Installation

<MijnUITabs items={["Npm", "Manual"]}>
  <MijnUITabsContent value="Npm">
  ```package-install
  @mijn-ui/react-alert
  ```
  </MijnUITabsContent>
  
  <MijnUITabsContent value="Manual">
  <Steps>
    <Step>
    **Copy and paste the following code into your project**

    ```json doc-gen:file
      {
        "file": "../../packages/components/alert/src/alert.tsx",
        "codeblock": {
          "lang": "tsx",
          "meta": "title=\"components/alert.tsx\""
        }
      }
    ```
    </Step>

    <Step>
    **Ensure that everything is functioning correctly after installation.**
    </Step>

  </Steps>
  </MijnUITabsContent>
</MijnUITabs>

## Usage

```tsx
import { Alert, AlertDescription, AlertTitle } from "@mijn-ui/react-alert"
```

```tsx
<Alert>
  <Terminal className="h-4 w-4" />
  <AlertTitle>Heads up!</AlertTitle>
  <AlertDescription>
    You can add components and dependencies to your app using the cli.
  </AlertDescription>
</Alert>
```

---

## Examples

### Alert Colors

The alert component offers five Colors: `success`, `info`, `warning`, `error`, and `default`:

<ComponentPreview name="alert-colors" className="md:p-10" />

### Alert Variants

The alert component is available in three variants: `default`, `outlined`, and `filled`:

#### Filled

<ComponentPreview name="alert-filled" className="md:p-10" />

#### Outlined

<ComponentPreview name="alert-outlined" className="md:p-10" />

---

## Alert Style Slots

| **Slot Name** | **Targeted Component** | **Purpose**                                   |
| ------------- | ---------------------- | --------------------------------------------- |
| `base`        | `Alert`                | Applies styles to the root `Alert` component. |
| `iconWrapper` | `Alert`                | Styles the wrapper around the alert icon.     |
| `title`       | `Alert`                | Styles the alert title.                       |
| `description` | `Alert`                | Styles the alert description text.            |
