---
"@mijn-ui/react-charts": minor
"@mijn-ui/react": minor
"@mijn-ui/react-core": patch
---

Add `@mijn-ui/react-charts` — `BarChart`, `GroupedBarChart`, `DonutChart`, `Sparkline`, and `ChartLegend` — plus categorical hue tokens in the core theme, and export the new package from the umbrella.
